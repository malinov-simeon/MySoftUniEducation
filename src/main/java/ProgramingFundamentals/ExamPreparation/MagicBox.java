import java.util.*;

public class MagicBox {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);

        ArrayDeque<Integer> =Arrays.stream(scanner.nextLine().split("//s+"))

    }
}
